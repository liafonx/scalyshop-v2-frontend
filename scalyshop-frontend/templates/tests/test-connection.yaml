apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scalyshop-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "scalyshop-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scalyshop-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
