apiVersion: v1
kind: Service
metadata:
  name: {{ include "scalyshop-frontend.fullname" . }}
  labels:
    {{- include "scalyshop-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5046
      protocol: TCP
      name: http
  selector:
    {{- include "scalyshop-frontend.selectorLabels" . | nindent 4 }}
